@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@using FCMS.ViewModels.Hospitals;
@model Hospitals
@{
    string isAdmin = string.Empty;
    var RolePermission = HttpContextAccessor.HttpContext.Session.Get<string>("SessionRoleAccess");
    if (RolePermission == "R")
    {
        isAdmin = "hide";
    }
}
<div class="row panel-body">
    <div class="col-lg-10 m-10">
        @*<input type="hidden" id="hrfConRefID" value='@Model.RefId' />*@
        @if (Model.HospitalsContactsList.Count != 0)
        {
            <table id="tblHospitalsContacts" class="table table-striped table-bordered table-hover">
                <thead>
                    <tr>

                        <th>Client Specific</th>
                        <th>Contact Name</th>
                        <th>Phone</th>
                        <th>Fax Number</th>
                        <th>Email ID</th>
                        <th>Bill Type</th>
                        <th>Instructions</th>
                        <th class="@isAdmin">Action</th>
                    </tr>
                </thead>
                <tbody>

                    @{int i = 0;
                        foreach (var item in Model.HospitalsContactsList)
                        {
                            i++;
                            <tr id="@item.ID">
                                <td id="txtClientName" class="tr1">@item.ClientName</td>
                                <td id="txtContactName" class="tr1">@item.ContactName</td>
                                <td id="txtPhone" class="tr1">@item.Phone</td>
                                <td id="txtFaxNumber" class="tr1">@item.FaxNumber</td>
                                <td id="txtEmailID" class="tr1">@item.EmailID</td>
                                 <td id="ddlBillType" class="tr1">@item.BillType</td>
                                <td id="txtInstructions" class="tr1">@item.Instructions</td>
                                <td class="tr1">
                                    <button id="btnContractEdit" type="button" Onclick="ConfirmHospitalContactEdit('@item.ID', '@item.ClientID', '@item.ContactName', '@item.Phone', '@item.FaxNumber', '@item.EmailID','@item.BillType','@item.Instructions')" title="Edit" class="fa fa-lg fa-edit  text-inverse form-control1 p-l-0 p-r-0 @isAdmin" style="border:none; background-color:transparent"></button>
                                    <button id="btnContractDelete" type="button" Onclick="ConfirmHospitalContactDelete('@item.ID', '@item.RefID')" title="Delete" class="fa fa-lg fa-trash-o  text-inverse form-control1 p-l-0 p-r-0 @isAdmin" style="border:none; background-color:transparent"></button>
                                </td>
                            </tr>
                        } }
                </tbody>
            </table>
        }
    </div>
</div>

