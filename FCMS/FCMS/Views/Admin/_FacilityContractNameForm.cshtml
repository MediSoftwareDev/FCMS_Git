@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@using FCMS.ViewModels.Admin;
@model FacilityContractNameDetails
@{
    string isAdmin = string.Empty;
    var RolePermission = HttpContextAccessor.HttpContext.Session.Get<string>("SessionRoleAccess");
    if (RolePermission == "R")
    {
        isAdmin = "hide";
    }
}
<div class="panel panel-inverse" data-sortable-id="form-validation-1">
    <div class="panel-heading">
        <div class="panel-heading-btn">
            <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default collapsed" id="expandcollapse" data-click="panel-collapse" data-original-title="" title="" data-init="true"><i class="fa fa-plus"></i></a>
        </div>
        <h4 class="panel-title" id="changeteam">Add Facility Contract Name</h4>
    </div>

    <form id="Contractform" method="post" class="form-horizontal">
        <div class="panel-body panel-form p-t-15 collapse m-t-10">
            <div class="form-group col-sm-6 col-md-6 col-lg-6">
                <label class="control-label col-md-4 col-sm-4" for="Name">Facility Contract Name<span style="color:red">*</span></label>
                <div class="col-md-6 col-sm-6 col-lg-8 controls" data-parsley-validate="true">
                    @Html.HiddenFor(m => m.FacilityContractNameDet.Id)
                    @Html.TextBoxFor(m => m.FacilityContractNameDet.Name, new { @class = "form-control", @placeholder = "Enter Facility Contract Name", data_parsley_required = "true" })
                    <span class="text-danger" id="spanContractst" hidden> Facility Contract Name Already Exist! Enter New Value</span>
                </div>
            </div>
            <div class="form-group col-sm-6 col-md-6 col-lg-6">

                <div id="errorcheck" style="padding-left:175px!important"></div>

            </div>
            <div class="form-group col-sm-12 text-center">
                <input type="button" id="btnSubmit" class="btn btn-sm btn-primary m-r-5 @isAdmin" value="Submit" />
                <input type="button" id="btnCancel" class="btn btn-sm btn-default m-r-5" value="Cancel" />
            </div>
        </div>
    </form>
</div>