@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@using WiseX.ViewModels.Admin;
@model Users
@{
    string isAdmin = string.Empty;
    var RolePermission = HttpContextAccessor.HttpContext.Session.Get<string>("SessionRoleAccess");
    if (RolePermission == "R")
    {
        isAdmin = "hide";
    }
}
@*@model IEnumerable<UserListInfo>*@
<input type="hidden" id="hfIsValidUser" value="@Model.IsValidUser.ToString()" />
<div class="panel panel-inverse" data-sortable-id="form-validation-1">
    <div class="panel-heading">
        <div class="panel-heading-btn">
            <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-default collapsed" id="expandcollapse" data-click="panel-collapse" data-original-title="" title="" data-init="true"><i class="fa fa-minus"></i></a>
        </div>
        <h4 class="panel-title">Users List</h4>
    </div>
    <div class="panel-body">
        <table id="tblUserList" class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Sno</th>
                    <th>Name</th>
                    <th>Role</th>
                    <th>Active</th>
                    <th>Created On</th>
                    <th>Last Login</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @{int i = 0;
                    foreach (var item in Model.UserList)
                    {
                        i++;
                <tr>
                    <td>@i</td>
                    <td>@item.UserName</td>
                    @*@if (item.UserRole.Contains(".&amp;"))
                    {
                        @item.UserRole.Replace(".&amp", "&")
                    }*@
                    <td>@Html.Raw(item.UserRole)</td>
                    <td>@item.Active</td>
                    <td>@item.CreatedOn</td>
                    <td>@item.ModifiedOn</td>
                    <td class="tr1">
                        <button id="btnedit" onclick="EditUser('@item.UserID')" title="Edit" class="fa fa-lg fa-edit @isAdmin  text-inverse form-control1" style="border:none; background-color:transparent"></button>
                        @if (item.Active != "No")
                        {
                            <button id="btnDelete" Onclick="ConfirmDelete('@item.UserID')" title="Delete" class="fa fa-lg @isAdmin fa-trash-o  text-inverse form-control1 p-l-0 p-r-0" style="border:none; background-color:transparent"></button>
                        }
                    </td>
                </tr>
                    } }
            </tbody>
        </table>
    </div>
</div>

